# Effort #8 tracker 约定

沿用 `docs/agents/issue-tracker.md` 本地 markdown 约定，effort 目录 = `.wayfinder8/`。
票号 T132 起；impl 切片 105 起。每轮：wayfinder 解票 → to-spec → to-tickets → implement → 验证（下游 `-am`）→ emoji commit。
