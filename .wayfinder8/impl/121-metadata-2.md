# impl-121 — 配置元数据增补（第二批）

**Status:** done

- [x] buzhou.index.closed-retention 入 core 元数据（默认 30d；-1/0 永久口径）
- [x] 绑定验证用例（属性解析 + Core props 归一；runtime 断言限定 ChatModel 场景外）

## Done

commit：见 git log（impl-121）。
