# CONTEXT

项目领域术语表（只收术语，不收实现细节）。

## 核心概念

- **Harness（马具）** — 挂载在 Spring AI 与业务 Agent 之间的运行时中间层；本项目的定位。叠加而非替代 Spring AI。
- **Agent 运行时（Agent Runtime）** — 让单个 Agent 稳定、可控、可解释地跑在生产里的那层能力：记忆、溢出保护、可观测、能力供给、并发执行。
- **会话（Session / Conversation）** — 一次 Agent 与用户的完整多轮交互，以 sessionId 标识，可跨实例续接。
- **轮次（Turn）** — 一次用户输入到 Agent 最终回复的完整往返；内部可能含多轮"思考—工具调用"递归。
- **完结轮次（Completed Turn）** — 工具调用链完整结束、不再有在途调用的轮次；微压缩的原子单位。
- **租户透明维度** — tenantId 在存储键命名空间/配置/配额/归因层透明贯穿、不进核心模型的多租户形态；调用方声明、框架不认证；隔离强度 = 命名空间级逻辑隔离（强隔离不做）；租户级配置 = 绑定级按租户实例化，不新增配置层。

## 记忆与压缩

- **渐进式压缩（Progressive Compaction）** — 信息从高精度原文连续、分级地降级到高密度摘要，永不断崖式丢弃。
- **微压缩（Micro-compaction）** — 纯内存、不调 LLM 的工具结果回收：旧工具返回替换为带证据指针的占位符。
- **证据指针（evidence-id）** — 微压缩占位符中指向持久化层原始工具返回的标识，供排障回查。
- **九段式摘要（Structured Summary）** —  LLM 按固定九段模板生成的结构化对话摘要，段落带优先级（P0 死保，P3 先砍）。
- **动态预算（Dynamic Budget）** — "先扣后算"：窗口减去输出预留、安全缓冲、系统提示词、工具 Schema、当前输入后，剩余才是历史预算。
- **悬空调用（Dangling Tool Call）** — 进程中断导致工具调用发出而结果未落库的残缺消息，加载历史时需自动修复。

## Spill

- **Spill（溢出保护）** — 超大工具返回值自动落盘持久化，上下文中只留预览 + 说明 + 回读路径。
- **回读（Read-back）** — 模型持 spill 路径主动取回数据，支持范围读取（字节区间 / JSON path / 分页）。

## 可观测

- **Span** — 有始有终、可嵌套的执行区间（会话 ⊃ 轮次 ⊃ 模型调用/工具调用）。
- **Event** — Span 内部的关键瞬间：思维链（Thinking）、最终回复、工具入参/出参、错误。
- **认知可观测（Cognitive Observability）** — 记录模型基于什么证据、做出什么推理、得到什么结论，而不只是"调用发生了"。
- **只读时间旅行** — 按轮还原"模型实际所见"（注入快照 + 消息 + 事件流），只查看不重跑；dashboard 呈现。
- **会话分叉（Session Fork）** — 从指定轮次复制消息历史到新 sessionId 续跑的轻量重放；承诺"同一注入视图与历史起点的新执行"，不承诺确定性复现；原轮次模型标识/参数记入 fork 元数据供对照。

## 质量验证

- **在线评估规则** — 生产流量质量评估的规则模型：过滤 × 采样 × 评估器（LLM-judge / 代码规则双型 SPI）；观测管线后置异步触发，结果回流 observability；无参考答案（区别于离线回归的数据集+阈值门禁）。
- **沙箱模式** — 绑定级运行时开关：ToolSetProvider 供给 mock 工具集 + 脚本模型，禁止真实接口触达；非部署形态。

## 能力供给

- **Skill（技能）** — 按需加载的能力单元；上下文只放清单（name + description），需要时再取正文。分内置（classpath）与 DB 动态两种来源，同名 DB 覆盖内置。
- **MCP 热插拔** — 工具集由配置驱动、运行时热更新；靠差量刷新 + 引用计数延迟关闭保证安全。
- **内容制品版本化** — prompt 模板与 Skill 正文统一的版本治理形态：不可变制品 + 钉版引用，DB 存储与覆盖复用 Skill 通道；版本进注入快照，回放可还原版本现场。
- **版本粘滞（Version Pinning）** — 灰度的状态机解法：spawn 时钉住制品版本、会话内不漂；老会话走旧版、新会话走新版；按百分比 canary 分流不做（网关/部署层职责）。
- **原子工具（Atomic Tools）** — 框架内置的最小可复用工具集：文件读写、命令执行、HTTP 调用、任务清单等。
- **计划自报（Self-reported Plan）** — 规划机制的官方形态：模型经 todo 工具自报工作清单，软约束（框架不强制按计划走）；计划审批 = HITL 门禁配置上 todo 即得（机制可拼装），无专门审批机制；强约束计划-执行分离架构出单 Agent 边界。

## Hook 护栏

- **Hook 链（Hook Chain）** — 框架在模型调用与工具调用前后暴露的 Callback 切面（beforeTool/afterTool/beforeModel/afterModel 等）；护栏逻辑挂于其上，与推理循环解耦。
- **引用句柄（Reference Handle）** — 长内容落盘后留在上下文中的指针文案（含路径与操作指引），LLM 凭句柄按需回读或编辑。
- **Onload（写侧加载）** — 工具执行前由 Hook 从文件加载长内容全文、覆盖工具入参的写侧护栏；与读侧 Offload 对称。
- **失败语义非对称** — 读侧 offload 失败降级透传（不阻断），写侧 onload 失败阻断调用（杜绝残缺产物外流）。
- **HITL 门禁（Dangerous Tool Guard）** — 配置驱动的危险工具拦截：未获真实用户授权，不可逆操作在框架层物理走不通；授权以 state 标记放行。
- **Hook→state→Attachment 闭环** — 补失忆范式：Hook 确定性采集事实写入会话 state，下一轮注入模型前以 Attachment 渲染进 prompt，不靠 LLM 自觉。
- **护栏三动作** — 内容安全护栏的可配动作：阻断 / 遮蔽 / 仅标记放行（事件留痕）；检测器自身故障降级透传（故障≠命中）。
- **假名化（Pseudonymization）** — 可逆映射脱敏（如 `PHONE_1` 占位）：会话内映射表保持一致使模型语义不断裂，映射表随会话生命周期销毁；与不可逆 redact 双模可配。

## 模型韧性

- **模型韧性层（Model Resilience）** — 模型调用的重试/超时/熔断/兜底机制；执行点挂 Advisor 链，错误感知走 Hook 链 onModelError 切面，事件进 observability。
- **归一化错误分类** — 跨 provider 统一的模型调用失败分类：限流/鉴权/内容/网络/未知；分类规则框架内置、可按 provider 扩展。
- **内容拒绝（Content Refusal）** — provider 内容过滤导致的静默拒绝（不抛异常，仅返回元数据标记）；韧性层只负责分类与可观测，治理策略归内容安全机制。
- **模型档位组（Model Tier Group）** — 用户声明的可互换模型集合；多模型路由只在组内 failover。责任划分：用户声明档位、框架校验能力、预算自动重估。
- **调用级路由** — 每次模型调用按熔断状态选择链位，熔断恢复自动回主；不做会话粘性切换。

## 持久化与恢复

- **持久化强度分档（Durability Tier）** — 会话状态落盘强度的按绑定级取舍：sync（下一步前同步落盘）/ async（默认，边执行边落盘）/ exit（仅退出时落盘）。
- **自动重驱动（Auto Resume）** — 崩溃恢复后无需用户输入、自动发起模型调用继续被中断轮次的 opt-in 恢复语义；默认语义为"轮次作废、等用户重驱动"。
- **幂等键（Idempotency Key）** — 副作用工具调用的去重标识（框架默认生成：会话+轮次+调用序号，业务可覆盖）；重试/重放命中键时返回首次结果而非重执行。
- **会话 Drain（会话排空）** — 优雅停机协议：停机信号后拒新会话、等在途会话当前轮次完结、超时强杀并落盘；粒度为轮次而非整个会话。会话跨实例续接靠持久化+租约天然接管，无显式迁移协议。
- **多层限流** — 框架在三个挂点的速率/并发管控：spawn 入口（实例级并发会话上限）、Advisor 层（每模型 RPM/TPM 双桶）、执行脊柱（每会话工具扇出上限）；管瞬时速率，累计配额归成本治理；网关入口 QPS 不归框架。
- **生命周期档位** — 会话数据 TTL 策略按绑定级分档（ephemeral/standard/long）+ 条目级覆盖；临时/持久/长短会话不立类型枚举、不进核心模型；清扫器内建于存储实现。
- **读时续期（refresh_on_read）** — 记忆类数据（摘要/state）的 TTL 默认策略：读取即续期，防长期活跃会话记忆被误删。
- **全链路删除** — 会话删除覆盖消息/摘要/state/租约/spill 落盘文件/观测事件/注入快照的硬删语义；spill 文件清理由框架负责；治理事件（审计族）与评估集样本豁免于会话删除；回放窗口 = 数据留存窗口。

## 失控防护

- **失控防护（Runaway Protection）** — 单轮内行为失控的检测与强制终止：数值硬顶（轮次×会话双窗口的步数/工具调用次数、单轮时长）+ 确定性重复检测（同工具同参数连续重复）；管行为失控，花费失控归成本治理。
- **软退出通道** — 达软阈值时经 Attachment 通道向模型注入"剩余步数"信号，让模型主动收尾而非硬撞上限；硬顶终止的受控终态携带部分结果。

## 成本治理

- **成本闸门** — 累计成本/token 的预算强制：会话级与绑定级两档，三级动作（软阈值告警 → 降级链切便宜模型 → 硬顶阻断）；软阈值经 Attachment 注入"预算余量"信号。管累计量，瞬时速率归多层限流；计费出账归业务系统。

## 工具治理

- **工具结果缓存** — 声明式缓存（键函数+TTL，可覆盖）；仅声明幂等的工具是"可缓存候选"；与幂等去重表分工——去重表防重放重复执行（正确性），缓存省重复调用（效率）。
- **工具熔断** — 工具连续失败后熔断该工具一段时间；与模型熔断状态机同族、作用域不同（工具级 vs 模型级）。
- **工具权限两层** — L1 结构权限（静态矩阵：主体×工具组 allow/deny，供给过滤+调用拦截双执行点）答"能不能"，L2 HITL（运行时个案授权）答"这次行不行"；主体上下文由调用方声明，框架不认证、只全链贯穿。

## 运行时干预

- **运行时干预平面** — 运行中会话的四能力控制平面：暂停/恢复、纠偏注入（改写工具入参或 state/prompt，复用 Attachment 通道）、kill switch（外部注入终止）、人工接管（operator 消息代答）；接管≠租约转让，干预经控制通道注入当前唯一活跃实例。
- **挂起-回填** — 工具调用（含 HITL 审批、长任务工具）挂起为 pending 态、外部事件（人工裁决/回调/webhook）回填后续跑的统一原语；状态可序列化外迁，等待不占线程。

## 审计

- **审计证据链** — 治理动作的不可变留痕体系；不建独立存储，是 observability 事件流的特化视图（观测与审计共用一份数据）；追加写、不可更新不可删（SPI 契约）；防篡改对接外部 WORM/日志服务，框架不重新发明。
- **治理事件** — 审计证据链记录的事件族：HITL 授权/权限判定/运行时干预/护栏命中/预算动作，统一 actor/action/target/outcome 四元组；区别于普通观测事件：同步落库、不采样、不受持久化强度分档降级。
