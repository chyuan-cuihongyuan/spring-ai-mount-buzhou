# Effort #9 tracker 约定

沿用 `docs/agents/issue-tracker.md` 本地 markdown 约定，effort 目录 = `.wayfinder9/`。
票号 T151 起；impl 切片 122 起。每轮：wayfinder 解票 → to-spec（spec 40 号起）→ to-tickets → implement → 验证（下游 `-am`）→ emoji commit。
