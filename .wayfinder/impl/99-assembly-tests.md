# impl-99 — 新能力 Spring 装配面断言

**What to build:** effort #6 能力的 auto-config 触发/属性绑定/条件降级四断言。

**Blocked by:** None

**Status:** done

- [x] 默认零装配（forwarder 无、限幅默认档）；webhook url→forwarder；限幅属性→Holder；index bean 共存
- [x] ToolResultLimiter.limitFor 转公共（生效上限查询面）
- [x] core 4/4 绿；spec 21 增补节

## Done

commit：见 git log（impl-99）。
