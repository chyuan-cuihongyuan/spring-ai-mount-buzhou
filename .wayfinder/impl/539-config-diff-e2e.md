# 539 — ConfigDiff×快照端点同源补验

**What to build:** 端点同源掩码快照 diff 用例（测试域轮）。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] 同源用例
- [x] spec 736 + README 行
- [x] 模块测试绿

## Done

验证：`mvn -pl buzhou-core -am test` 绿。commit 见本轮 `test(core)` 提交。
