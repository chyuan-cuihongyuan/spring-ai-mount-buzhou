# 848 — compact×evidence 交叉组合测试轮

**What to build:** CompactEvidenceComboTest——压缩前后回查一致 + 双读面守恒。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] CompactEvidenceComboTest（压缩前/后回查一致 + 双守恒三测）
- [x] spec 1096 + README 行

## Done

验证：`mvn -pl buzhou-memory -am test -Dtest='CompactEvidenceComboTest'` 全绿。commit 见本轮 `test(memory)` 提交。
