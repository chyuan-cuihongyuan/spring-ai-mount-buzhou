# 863 — evidence×episodic 独立性组合测试轮

**What to build:** EvidenceEpisodicComboTest——交叉调用 + 双读面独立 + reset 独立。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] EvidenceEpisodicComboTest（交叉/独立/隔离三测）
- [x] spec 1111 + README 行

## Done

验证：`mvn -pl buzhou-memory -am test -Dtest='EvidenceEpisodicComboTest'` 全绿。commit 见本轮 `test(memory)` 提交。
