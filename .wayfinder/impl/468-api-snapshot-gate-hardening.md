# 468 — API 快照门硬化

**What to build:** regenerateSnapshot 门控（@EnabledIfSystemProperty）+ 显式再生补录 7 新公共型。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] 门控注解 + Javadoc 更新流程改写
- [x] 快照显式再生（7 型补录）
- [x] starter 套件 11 绿 + 1 skipped 验证
- [x] spec 615 + README 行

## Done

验证：`mvn -pl buzhou-spring-boot-starter -am test` 绿。commit 见本轮 `test(starter)` 提交。
