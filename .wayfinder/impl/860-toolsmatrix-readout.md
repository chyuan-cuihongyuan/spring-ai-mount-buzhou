# 860 — tools 五读面全矩阵组合测试轮

**What to build:** ToolsMatrixReadoutTest——五读面交叉 + 各自守恒 + 互不串账 + reset 独立。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] ToolsMatrixReadoutTest（五交叉/各自守恒/隔离三测）
- [x] spec 1108 + README 行

## Done

验证：`mvn -pl buzhou-tools -am test -Dtest='ToolsMatrixReadoutTest'` 全绿。commit 见本轮 `test(tools)` 提交。
