> **历史存档**：以下为 effort #10 运行期的 tracker 约定（当时每个 effort 一个独立目录 `.wayfinder10/`）。
> 2026-08-17 起全部 effort 融合为单一 `.wayfinder/`：map → `maps/effort-10.md`，决策票 → `../tickets/`，实现切片 → `../impl/`，现行约定见 [`../README.md`](../README.md)。

# Effort #10 tracker 约定

沿用 `docs/agents/issue-tracker.md` 本地 markdown 约定，effort 目录 = `.wayfinder10/`。
票号 T170 起；impl 切片 139 起。每轮：wayfinder 解票 → to-spec（spec 46 号起）→ to-tickets → implement → 验证（下游 `-am`）→ emoji commit。
