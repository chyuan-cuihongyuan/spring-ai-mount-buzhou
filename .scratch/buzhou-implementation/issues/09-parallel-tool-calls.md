# 09 — 并行工具调用

**What to build:** HarnessToolCallingManager 经公开扩展点注入：同轮多 tool_call 虚拟线程 fan-out、结果严格按原序回注；会话级执行器+每轮上限 8（信号量）；单工具超时 60s、失败/超时转文本回注、取消传播；声明式串行组（组级互斥按原序排队）；运行期瞬断重试独立可配（默认 0，指数退避上限 3）。

**Blocked by:** 04

**Status:** ready-for-agent

- [ ] 三工具并行总耗时≈最长单个（而非相加）有测试
- [ ] 结果回注顺序与 tool_call 顺序一致（快慢乱序完成场景）
- [ ] 一工具超时/失败不影响同轮其他调用，失败文本回注
- [ ] 串行组内不并发；轮次取消传播到全部在途调用
