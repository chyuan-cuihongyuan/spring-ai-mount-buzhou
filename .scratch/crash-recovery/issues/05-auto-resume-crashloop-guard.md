# 05 — AUTO_RESUME opt-in 续跑 + 崩溃循环兜底

**What to build:** opt-in **自动重驱动**——加载+悬空修复后，若历史结尾为「被中断轮次」（无终结性助手回复），**无需用户输入**重新发起模型调用续跑该轮；默认仍为 VOID（不擅自续跑）。M1 提供**崩溃循环硬顶兜底**（03/04 韧性层熔断就绪前的保守闸门），并记续跑/跳过事件。续跑对副作用安全由 04 的恢复幂等保证。

**Blocked by:** 04（恢复幂等——让续跑对副作用安全）

**Status:** done

- [ ] opt-in `AUTO_RESUME`：加载+修复后，历史结尾为被中断轮次（无终结性助手回复）时，自动发起模型调用续跑、无需用户输入
- [ ] 默认 `VOID`：未 opt-in 时绝不自动续跑（与 01 的默认行为一致）
- [ ] 「被中断轮次」判定准确：完结轮次（已有终结回复）不续跑；多轮工具循环中途崩溃、流式崩溃等边界按作废/续跑预期处理
- [ ] 崩溃循环硬顶：自动重驱动反复崩溃时按硬顶次数掐断（M1 兜底），记 `resume-skipped-crashloop` 事件
- [ ] 续跑成功记 `turn-recovered{action=auto-resumed}` 事件
- [ ] `resume-strategy` 可经 yml 配置（`VOID` 默认）；提供一键关自动重驱动开关
