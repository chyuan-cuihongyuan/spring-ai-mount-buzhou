# 05 — 机制文档 + docs/spec 同步 + 运维续接模式收口

**What to build:** 机制详设文档落盘并同步 docs/spec（「改机制先改 Spec」：drain 三步协议、会话台账、SmartLifecycle 相位、配置项全表）；「滚动发布会话续接模式」运维章节（drain → 释租约 → 实例 B 同 sessionId spawn → 05 恢复语义加载历史续接）作为「不做显式跨实例迁移」决策的标准答案；诚实边界写明（模型调用阶段无取消句柄、强杀只到工具层）；M2 预留位写明（15 运行时干预复用轮次边界等待原语、21 按租户 drain、与 07 背压共用拒新即路由信号语义）；safe-by-default 审计收口（默认开、默认超时派生、一键关闭开关核验）；路线图 M1 行勾选 06 项。

**Blocked by:** 04（全部机制与装配收口后）

**Status:** ready-for-agent

- [ ] 机制详设文档落盘，与实现一致（drain 协议 / 台账 / 相位 / 配置项全表 / 事件类型全表）
- [ ] docs/spec 同步（09 模块工程 + 08 会话/持久化相关章节按需更新）
- [ ] 运维章节含「滚动发布会话续接模式」标准流程（drain → 释租约 → 实例 B 同 sessionId spawn）
- [ ] 模型调用不可强中断边界、相位关系与取值依据写明
- [ ] M2 预留位（15 / 21 / 07 联动点）写明
- [ ] safe-by-default 审计：默认装配开、默认超时派生、关闭开关有效
- [ ] docs/production-readiness/README.md 路线图 M1 的 06 项标注已落地
